import React from 'react';
import { LotList } from '../features/lots/components/LotList';

const Lots: React.FC = () => {
  return (
    <div className="p-4">
      <LotList />
    </div>
  );
};

export default Lots;
